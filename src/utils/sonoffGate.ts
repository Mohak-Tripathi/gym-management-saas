export const triggerSonoff = async (deviceId?: string) => {
    console.log(`🚪 Triggering Sonoff device [${deviceId}] to open gate...`);
    // You can integrate real Sonoff HTTP/ESP API here later
  };